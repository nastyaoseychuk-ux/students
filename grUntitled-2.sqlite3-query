-- database: ./student.db

CREATE TABLE students (
    student_id INT,
    name VARCHAR(50),
    age INT
);

CREATE TABLE age (
    student_id INT,
    subject VARCHAR(50),
    grade FLOAT
);

INSERT INTO students (student_id, name, age) VALUES
(1, 'Anna', 19),
(2, 'Ivan', 20),
(3, 'Oleh', 21),
(4, 'Maria', 18),
(5, 'Dmytro', 22);

INSERT INTO age (student_id, subject, grade) VALUES
(1, 'Math', 10.5),
(1, 'English', 11.0),
(2, 'Math', 9.0),
(2, 'English', 10.0),
(3, 'Math', 8.0),
(3, 'English', 9.0),
(4, 'Math', 12.0),
(4, 'English', 11.5),
(5, 'Math', 7.0),
(5, 'English', 8.0);

SELECT age.subject, AVG(age.grade) AS average_grade
FROM students
JOIN age
    ON students.student_id = age.student_id
WHERE students.age <= 20
GROUP BY age.subject;